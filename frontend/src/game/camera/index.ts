/**
 * @file Runtime del juego: index.
 */

export {
  getCameraController,
  isCameraInspectMode,
  setCameraController,
  type CameraControllerRef,
} from './camera-access';
/** Re-export de símbolos del módulo. */
export { CameraController } from './camera-controller';
/** Re-export de tipos del módulo. */
export type { CameraMode } from './camera-types';
