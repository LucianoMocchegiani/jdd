/**
 * @file Punto de entrada del núcleo ECS (reexportaciones públicas).
 */

export { Component } from '@/ecs/core/component';
/** Re-export de símbolos del módulo. */
export { System } from '@/ecs/core/system';
/** Re-export de símbolos del módulo. */
export { World, type EntityId } from '@/ecs/core/world';
